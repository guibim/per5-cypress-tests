// Quando o site for revisado e tiver formulário, reativar este teste utilizando API real ou mock

describe('Formulário de Contato', () => {
  it('não há formulário nesta landing, teste pulado', function () {
    this.skip();
  });
});